<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sr.unasat.test">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test">
        <activity android:name=".SummaryActivity" />/activity>
        <activity android:name=".AntroewaActivity"/>
        <activity android:name=".BitawiriActivity" />
        <activity android:name=".KoolActivity" />
        <activity android:name=".BoulangerActivity" />
        <activity android:name=".KlaroenActivity" />
        <activity android:name=".KousebandActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".PompoenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".Database.OrderProvider"
            android:authorities="sr.unasat.test.data.OrderProvider"
            android:exported="true" />
    </application>

</manifest>